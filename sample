pipeline {
    agent any
    stages {
        stage('java version') {
            steps {
                sh 'echo java version'
                sh 'java -version'
            }
        }
        stage('maven version') {
            steps {
                sh 'echo maven version'
                sh 'mvn -version'
            }
        }
    }
}
